{
  "name": "research-paper-validator",
  "version": "1.0.0",
  "description": "Blockchain-based research paper validator",
  "main": "index.html",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia",
    "test": "npx hardhat test",
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  }
}